const DATES = ["SUN", "MON", "TUE", "WED", "THU", "FRI", "SAT"];

const AvailabilityTab = () => {
  return (
    <div className="mt-5  min-h-[13rem] w-full bg-red-400">
      {DATES.map((data) => {
        return <div key="1">123</div>;
      })}
    </div>
  );
};

export default AvailabilityTab;
